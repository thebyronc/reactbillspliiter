import React from 'react';
import { Text } from 'react-native';
import Styles from '../styles/styles';

export default class TestImport extends React.Component {
  render() {
    return (
      <Text style={Styles.bodyText}>Example Test Import Content</Text>
    );
  }
}
